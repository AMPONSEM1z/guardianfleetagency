/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://guardianfleetagency.com", // ✅ your custom domain
  generateRobotsTxt: true, // also creates robots.txt
  sitemapSize: 7000, // split if too many URLs
};
